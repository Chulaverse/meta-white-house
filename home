<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MetaWhiteHouse</title>
    <style>
        /* Body Styling */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-image: url('path_to_your_image/A_futuristic_digital_hub_called_MetaWhiteHouse,_se.png');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            background-attachment: fixed;
            color: #ffffff;
            text-align: center;
        }

        /* Overlay to Darken Background */
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.6); /* Slightly darken the background */
            z-index: -1;
        }

        /* Main Section Styling */
        .content {
            padding: 100px 20px;
        }

        .content h1 {
            font-size: 4em;
            margin-bottom: 20px;
        }

        .content p {
            font-size: 1.5em;
            margin-bottom: 50px;
        }

        /* Featured Projects Section */
        .projects {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .project-box {
            background-color: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            width: 300px;
        }

        .project-box h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
        }

        .project-box p {
            font-size: 1em;
        }

        /* Metahumans Section */
        .metahumans {
            margin-top: 50px;
        }

        .metahumans h2 {
            font-size: 2.5em;
            margin-bottom: 30px;
        }

        .metahuman-cards {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .metahuman-box {
            background-color: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            width: 300px;
        }

        .metahuman-box h3 {
            font-size: 1.5em;
        }

        .metahuman-box p {
            font-size
